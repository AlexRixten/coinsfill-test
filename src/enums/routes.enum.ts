export const enum ERoutes {
  Main = "/",
  Auth = "/signin",
  Card = "/card",
  Next = "/next",
  Wallet = "/wallet",
  Profile = "/profile",
  Settings = "/settings",
  UploadAvatar = "/settings/upload-avatar",
}
